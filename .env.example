# Funded Robinhood Chain testnet wallet (deploys contracts, runs the relayer and the agent)
PRIVATE_KEY=
DEPLOYER=

# Optional: turns on the agent's Claude news analyst
ANTHROPIC_API_KEY=

# Optional agent overrides (defaults in apps/agent/src/config.ts)
# AGENT_DEPTH=20
# AGENT_MAX_SPEND_PER_TRADE=5
# AGENT_MAX_EXPOSURE=25
# AGENT_SIGMA_PCT=2.0
# AGENT_BTC_BETA=0.3
# AGENT_DRY_RUN=false
